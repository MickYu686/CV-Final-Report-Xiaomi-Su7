#Preparation for Testing
Put your test images/videos into the detect-test folder
(the folder is already created in the project):
├── detect-test/          # Test data folder   
    ├── img1.jpg          # Test image 1
    ├── video1.mp4        # Test video 1
    └── ...
Supported data formats: 
JPG/PNG (images), MP4/AVI (videos), even camera streams (use source=0 for webcam).

#Run Test Command
Use Ultralytics CLI
# Basic test (use yolov8n.pt, detect-test folder as source)
yolo detect predict model=yolov8n.pt source=detect-test

# Customized test (e.g., save results to specific folder, set confidence threshold)
yolo detect predict model=yolov8n.pt source=detect-test save=True conf=0.5 project=./runs/detect name=test_exp

#Check Test Results
(images/videos with bounding boxes) are saved in the runs/detect/ folder (auto-created):
runs/
└── detect/
    ├── predict        
    │   └── img.jpg
    .
    .
    .
    ├── predict6           
    │   └── su7 video 1     
    └── ...
